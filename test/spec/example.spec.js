'use strict';

var expect = require('chai').expect;

describe('example', function() {
    it('helps us to remember what a test looks like ;)', function() {
        expect(true).to.equal(true);
    });
});